DATABASE_URL=sqlite:///./sentinel.db
MLFLOW_TRACKING_URI=http://localhost:5000
MODEL_PATH=models_saved/best_model.pt
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO

# Optional. When set, the API requires an X-API-Key header matching this
# value on all /api/v1/predict endpoints. Leave blank to disable auth.
SENTINEL_API_KEY=

# Optional. Streamlit dashboard alert webhook (Phase 3 alert simulation).
SLACK_WEBHOOK_URL=
